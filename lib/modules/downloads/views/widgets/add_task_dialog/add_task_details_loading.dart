import 'package:flutter/material.dart';

class AddTaskDetailsLoading extends StatelessWidget {
  
  
  const AddTaskDetailsLoading({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}